#numpy Array iterating
#iterating arrays
#iterating means going through elements one by one.

#if we iterate on a 1-D array it ill go through each elements one by one

#iterate on the elements of the following 1-D array?

import numpy as np

arr=np.array([1,2,3])

for x in arr:
    print(x)